checks:
    javascript: true

build:
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run

    tests:
        override:
            command: 'npm test'
            coverage:
                file: 'coverage/clover.xml'
                format: 'clover'
